import { Tweet } from "@/components/tweet/Tweet";
import React from "react";

interface GlanceProps {
  url: string;
}

const Glance: React.FC<GlanceProps> = ({ url }) => {
  return <>{/* <Tweet id={}/> */}</>;
};

export default Glance;
